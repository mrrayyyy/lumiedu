# Skeleton Nginx config for reverse-proxy
# TODO(config-ready): add upstream routing, websocket, TLS and security headers.

server {
    listen 80;
    server_name _;

    location / {
        return 200 "lumiedu reverse-proxy skeleton\n";
        add_header Content-Type text/plain;
    }
}
