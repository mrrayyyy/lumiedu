# Skeleton Dockerfile for STT service (Faster-Whisper adapter)
# TODO(config-ready): install model/runtime and expose API endpoint.

FROM python:3.11-slim

WORKDIR /app

# Placeholder command for skeleton phase
CMD ["sh", "-c", "echo 'stt skeleton container' && sleep infinity"]
